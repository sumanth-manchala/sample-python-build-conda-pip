name: Run Lint Workflow with Pixi

run-name: Lint with pixi - ${{ github.event.head_commit.message }}
on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  lint:
    uses: ./.github/workflows/lint.yaml
    with:
      lint_command: "pixi run lint"
      conda_install_tool: pixi
      build_command: "pixi build"